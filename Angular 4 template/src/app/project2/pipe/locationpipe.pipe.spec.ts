import { LocationpipePipe } from './locationpipe.pipe';

describe('LocationpipePipe', () => {
  it('create an instance', () => {
    const pipe = new LocationpipePipe();
    expect(pipe).toBeTruthy();
  });
});
